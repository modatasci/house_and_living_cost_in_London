# Copy to .env and fill in. Never commit .env.
TFL_APP_KEY=
# Comma-separated allowed origins for CORS (frontend dev + prod).
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Path to data dir (defaults to ../../data relative to api/)
DATA_DIR=
